[package]
name = "curl-tui"
version = "0.1.0"
edition = "2021"

[dependencies]
curl-tui-core = { path = "../curl-tui-core" }
ratatui = "0.29"
crossterm = "0.28"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
